# Use the following command to install the Fira Code font.

sudo apt update
sudo apt install fonts-firacode

# After the installation is complete, you can verify that Fira Code
# is installed by checking the fonts in your text editor or any 
# other application that lists available fonts.